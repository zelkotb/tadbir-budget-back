# BPMN processes

Drop your `*.bpmn20.xml` process definitions here. Flowable auto-deploys every file in this
directory on startup (`flowable.check-process-definitions=true`). This base project ships no
process of its own — add one per business workflow you build on top of the engine.
